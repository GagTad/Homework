int alternatingCharacters(string s) {
    int q=0;
    for(int i=0;i<s.size();++i)
        {
            if(s[i]==s[i+1])
            {
               q++; 
            }
        }
        return q;
}
